# 上游系统管理数据库执行记录

日期：2026-06-04

状态：已执行并完成首轮验证。

## 数据源确认

- 配置读取时间：2026-06-04
- 后端激活配置：`RuoYi-Vue/ruoyi-admin/src/main/resources/application.yml`
- 激活 profile：`druid`
- 数据源配置：`RuoYi-Vue/ruoyi-admin/src/main/resources/application-druid.yml`
- MySQL 连接来源：`.env.local` 中的 `RUOYI_DB_URL`、`RUOYI_DB_USERNAME`、`RUOYI_DB_PASSWORD`
- Redis 连接来源：`.env.local` 中的 `RUOYI_REDIS_HOST`、`RUOYI_REDIS_PORT`、`RUOYI_REDIS_DATABASE`、`RUOYI_REDIS_PASSWORD`
- 本地 Docker MySQL/Redis：本次未作为默认数据源，不读取、不写入
- 敏感信息处理：本文档不记录数据库地址全文、账号、密码、Redis 密码、领星 Key、领星 Secret

## 用户授权

- 用户已确认允许执行 SQL。
- 用户已确认使用真实领星主仓 `CA012` 做授权和同步验证。
- 用户已确认真实领星凭证保留在系统中，不需要测试后删除。
- 凭证落库要求：必须加密保存、脱敏展示，不允许明文写入源码、SQL 文件、日志、前端响应或报告。

## 计划执行类型

| 类型 | 是否影响远端数据 | 说明 |
| --- | --- | --- |
| DDL | 是 | 新增上游系统管理相关业务表和索引 |
| DML | 是 | 新增/更新字典、菜单、按钮权限、初始主仓接入 |
| 查询 | 否 | 查询菜单、表结构、执行结果和同步数据 |
| 真实接口调用 | 可能间接产生外部请求日志 | 调用领星授权、仓库、物流渠道、SKU 同步接口 |

## 执行明细

| 时间 | 动作 | 命令类型 | 目标 | 结果 |
| --- | --- | --- | --- | --- |
| 2026-06-04 01:39 | 新增上游系统管理 SQL | DDL/DML | 当前激活 MySQL | 已执行 `RuoYi-Vue/sql/upstream_system_management_seed.sql`，JDBC runner 回报 `sql-statements-executed=20` |
| 2026-06-04 01:42 | 写入真实主仓接入 | API + DML | 当前激活 MySQL | 已通过后端接口保存 `LX-CA012`，凭证加密落库，前端仅展示脱敏 Key |
| 2026-06-04 01:49 | 真实授权和同步 | 外部 API + DML | 领星接口 / 当前激活 MySQL | 已同步仓库 1 个、物流渠道 7 条、SKU 5400 条 |
| 2026-06-04 02:05 | 同步性能修正后复测 | 外部 API + DML | 领星接口 / 当前激活 MySQL | SKU 按页批量 upsert 后真实同步通过，避免单条写入导致超时 |
| 2026-06-04 02:08 | 仓库配对约束验证 | API + DML | 当前激活 MySQL | 临时仓库配对创建成功、重复系统仓库被拒绝、临时配对已删除 |
| 2026-06-04 02:09 | 物流渠道配对约束验证 | API + DML | 当前激活 MySQL | 同一领星渠道可配多个系统渠道，重复系统渠道被拒绝，临时配对已删除 |
| 2026-06-04 02:16 | SKU 配对约束验证 | API + DML | 当前激活 MySQL | 临时 SKU 配对创建成功、重复配对被拒绝、临时配对已删除；保留只追加审计事件 |
| 2026-06-04 02:17 | 权限验证 | API 查询 | 当前后端 | `admin` 可访问；默认 `ry` 用户访问上游系统列表返回 403 |
| 2026-06-04 02:18 | 请求日志脱敏抽查 | API 查询 | 当前激活 MySQL / 当前后端 | 前 5 条日志无 `appSecret` 或凭证字段，`authcode` 字段为脱敏内容 |

## 执行结果汇总

- 主仓接入：`LX-CA012` 已保留在系统中，凭证为加密保存。
- 同步清单：仓库 1 个、物流渠道 7 条、SKU 5400 条。
- 临时验证数据：仓库、物流渠道、SKU 临时配对均已删除；请求日志和 SKU 配对审计事件按只追加规则保留。
- 菜单权限：已写入“海外仓服务设置 / 上游系统管理”菜单和按钮权限。

## 回滚思路

- 菜单和权限：可通过禁用对应 `sys_menu` 记录或删除本次新增菜单记录回滚。
- 字典：可禁用本次新增字典数据；不建议物理删除已有业务引用的 code。
- 业务表：如尚无业务引用，可按 SQL 反向删除；如已经发生真实配对或请求日志，优先停用接入并保留日志，不做物理删除。
- 真实领星凭证：如需废弃，优先停用主仓接入并清空加密凭证字段，不删除请求日志。
